[phases.setup]
nixPkgs = ["php82", "composer", "nodejs_18", "pkg-config", "openssl", "zlib"]

[phases.build]
cmds = [
  "composer install",
  "npm install",
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"